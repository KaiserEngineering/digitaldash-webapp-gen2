{
  "installCommand": "corepack enable && corepack prepare yarn@4.14.1 --activate && yarn install",
  "buildCommand": "corepack enable && corepack prepare yarn@4.14.1 --activate && yarn run build:vercel"
}
